"""
Commit the data for hashing
"""
from constants import BASE_DIR, GOD_DIR, HASH_DIR
from pathlib import Path

import hashlib


def commit():

    # Collect files
    directories = Pathlib(BASE_DIR).grob("")

    # Calculate hash


    # Move objects to hash directory


    # Create symlink


    # Record the add
